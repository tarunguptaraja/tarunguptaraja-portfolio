{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "deploy": "cd .output/server && rd /s /q node_modules && npm i && cd .. && firebase deploy",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test": "vitest"
  },
  "devDependencies": {
    "firebase-admin": "^11.2.0",
    "nuxt": "3.0.0",
    "vitest": "^0.25.3"
  },
  "dependencies": {
    "@highlightjs/vue-plugin": "highlightjs/vue-plugin",
    "@nuxtjs/tailwindcss": "^6.1.3",
    "aos": "^3.0.0-beta.6",
    "firebase": "^10.10.0",
    "firebase-functions": "^5.0.1",
    "highlight.js": "^11.7.0"
  }
}
